plugins{
	id "org.sonarqube" version "2.6"
}

ext.projectIds = [
	'group': 'com.manning.gia',
	'version': 0.1
]

//group = projectIds.group
//version = projectIds.version

allprojects{
	group = projectIds.group
	version = projectIds.version
}

subprojects{
	apply plugin: 'java'
}


